// import SubCategory from './SubCategory';
// import Card from './Card';

// export default function Category({ title, data, onChange }) {
//   const updatedData = { ...data };

//   const content = Array.isArray(data) ? (
//     <SubCategory title={title} data={updatedData} />
//   ) : (
//     <Card item={updatedData} />
//   );

//   function handleEditClick() {
//     // set mode of category to edit
//     // hide all other categories until save
//     // click save saves all cards
//     // if on disable next
//   }

//   return (
//     <div className="category personal">
//       <h2 className="category-title">{title}</h2>
//       {content}
//     </div>
//   );
// }
